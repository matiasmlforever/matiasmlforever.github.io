name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: node:bullseye-slim

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install

    - name: Build the app (predeploy)
      run: npm run predeploy

    - name: Deploy to GitHub Pages
      run: npm run deploy
      with:
        branch: main
        folder: build
